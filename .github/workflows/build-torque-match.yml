name: Build Torque Match APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'torque-match/**'
      - '.github/workflows/build-torque-match.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: torque-match
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
      - name: Install Android platform
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Apply challenge chapter system
        run: python3 tools/patch_challenges.py
      - name: Fix challenge renderer compile collision
        run: python3 tools/fix_challenge_compile.py
      - name: Apply part-specific audio hooks
        run: python3 tools/patch_audio_hooks.py
      - name: Generate automotive audio bank
        run: python3 tools/generate_audio.py
      - name: Build debug APK
        run: gradle --no-daemon assembleDebug
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk TorqueMatch-v0.6.0-debug.apk
      - name: Package exact built source
        working-directory: .
        run: zip -r TorqueMatch-v0.6.0-Source.zip torque-match -x 'torque-match/.gradle/*' 'torque-match/app/build/*'
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TorqueMatch-v0.6.0-APK
          path: torque-match/TorqueMatch-v0.6.0-debug.apk
          if-no-files-found: error
          retention-days: 30
      - name: Upload source
        uses: actions/upload-artifact@v4
        with:
          name: TorqueMatch-v0.6.0-Source
          path: TorqueMatch-v0.6.0-Source.zip
          if-no-files-found: error
          retention-days: 30
