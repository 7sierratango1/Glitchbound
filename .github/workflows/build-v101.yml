name: Build Glitchbound v1.0.1

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-v101.yml'
      - '.build/v101_patch.py'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reconstruct v0.8.3 baseline
        run: cat .build/v083.part1 .build/v083.part2 .build/v083.part3 | base64 -d | gzip -d > app/src/main/assets/www/game.js

      - name: Normalize v1.0.0 patch matching
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('.build/v100_patch.py')
          s=p.read_text()
          s=s.replace("must(old,new,'choice caps')", "s,n=re.subn(r\"let pool=\\[\\.\\.\\.SKILLS\\].*?,el=\\$\\('#choiceList'\\);\",new,s,count=1); n==1 or (_ for _ in ()).throw(SystemExit('missing choice caps'))")
          p.write_text(s)
          PY

      - name: Apply v1.0.0 systems
        run: python3 .build/v100_patch.py

      - name: Apply v1.0.1 systems
        run: |
          python3 .build/v101_patch.py
          node --check app/src/main/assets/www/game.js
          sed -i "s/versionCode 10/versionCode 21/" app/build.gradle
          sed -i "s/versionName '0.8.1'/versionName '1.0.1'/" app/build.gradle

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build APK
        run: gradle assembleDebug --no-daemon

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Glitchbound-v1.0.1-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Glitchbound-v1.0.1-APK
          path: Glitchbound-v1.0.1-debug.apk
          retention-days: 90
